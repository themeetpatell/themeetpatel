# Resend (server-side only — for form notifications)
RESEND_API_KEY=re_xxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev

# Neon Database (server-side only — never prefix with VITE_)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Admin Auth (server-side only)
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD=your_admin_password
JWT_SECRET=random_64_char_hex_string

# Vercel Blob (for media uploads — server-side only)
BLOB_READ_WRITE_TOKEN=your_blob_token
